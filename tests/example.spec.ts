import { test, expect } from "@playwright/test";

// Group 1 - Home Page
// Test metadata and elements (e.g. h1 in hero? maybe more?)

// Group 2 - Navbar?
// Test "logowanie" link and logo (check if it is a link and takes us to /logowanie or / page)

// Group 3 - Logowanie
// Test if input[placehoder="..."] exists and button with specific name

// Group 4 - Zlecenia
// Test if zlecenia list is empty when no tasks are created in db, test if tasks are displayed in zlecenia list once added
// use data-testId="" on list and item
